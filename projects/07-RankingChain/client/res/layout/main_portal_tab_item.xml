<?xml version="1.0" encoding="utf-8"?>
<!-- 移动通登陆成功后的入口activity的tabHost的item布局实现. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="match_parent"
    android:background="@drawable/main_portal_tab_selector"
    android:gravity="center"
    android:orientation="vertical"
    android:paddingBottom="0dp"
    android:paddingLeft="0dp"
    android:paddingRight="0dp"
    android:paddingTop="0dp"><!--android:paddingTop="3dp"-->

    <ImageView
        android:id="@+id/main_portal_tab_item_iconView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:focusable="false"
        android:padding="0dp"
        android:layout_marginTop="2dp"
        android:src="@drawable/main_portal_tab_bg_mynotice">
    </ImageView>


    <TextView
        android:id="@+id/main_portal_tab_item_textView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:ellipsize="marquee"
        android:singleLine="true"
        android:layout_marginTop="3dp"
        android:textColor="@color/main_portal_tab_item_textcolor"
        android:textSize="11sp"
        android:text="消息">
    </TextView>

</LinearLayout>